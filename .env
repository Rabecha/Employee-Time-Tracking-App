# Database settings (used by the app)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=time_tracking_app

# MySQL root password (used by docker-compose mysql service)
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=time_tracking_app

# App settings
PORT=3000
NODE_ENV=development
